import { shell, feedbackNode, setFeedback, el, shuffle } from './activity-utils.js';
export function render(activity, ctx) { const root=shell(activity), answer=(activity.answer||activity.options||[]).join?.('')||String(activity.answer||''), selected=[]; const bank=el('div',{className:'letter-bank'}), line=el('div',{className:'sentence-line'}), feedback=feedbackNode(); shuffle(activity.options||[...answer]).forEach(letter=>{const b=el('button',{className:'letter-tile',type:'button',text:letter});b.onclick=()=>{if(b.disabled)return;b.disabled=true;selected.push(letter);line.append(el('span',{className:'builder-slot',text:letter}));if(selected.length===answer.length){const ok=selected.join('')===answer;ctx.onAttempt?.(ok,activity);if(ok){setFeedback(feedback,'הסדר נכון!',true);ctx.onComplete?.({correct:true,points:activity.points||0});}else setFeedback(feedback,'כמעט! נסו לאפס ולסדר מחדש.');}};bank.append(b)}); const reset=el('button',{className:'btn btn-ghost',type:'button',text:'איפוס',onclick:()=>{selected.length=0;line.replaceChildren();[...bank.children].forEach(b=>b.disabled=false);}});root.append(line,bank,reset,feedback);return root; }
